import 'package:flutter/material.dart';

const m20H = EdgeInsets.symmetric(
  horizontal: 20,
);
const marginV = EdgeInsets.symmetric(
  vertical: 20,
);
const marginHV20 = EdgeInsets.all(
  20,
);
const marginHV10 = EdgeInsets.symmetric(
  vertical: 10,
  horizontal: 20,
);