import 'package:flutter/material.dart';


const p20h = EdgeInsets.symmetric(horizontal: 20);

const appbarPadding = EdgeInsets.only(
  top: 50,
  bottom: 10,
  left: 20,
  right: 20,
);
const marginHV5 = EdgeInsets.symmetric(
  vertical: 5,
  horizontal: 20,
);
const marginHV8 = EdgeInsets.symmetric(
  vertical: 8,
  horizontal: 20,
);